name: Lab 0 Infrastructure Test

on: [push, pull_request]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test-infrastructure:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: ecommerce
        ports:
          - 5432:5432
        # Виправлено: явно додаємо прапорець -U postgres для перевірки здоров'я
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Run Application and Test Infrastructure
      env:
        DB_HOST: localhost
        DB_PORT: 5432
        DB_NAME: ecommerce
        DB_USER: postgres
        DB_PASSWORD: password
      run: |
        echo "=== 1. STARTING APPLICATION ==="
        uvicorn main:app --port 8000 &
        APP_PID=$!
        sleep 5 

        echo -e "\n=== 2. TESTING HEALTH CHECK (DB ONLINE - 200 OK) ==="
        curl -i http://localhost:8000/health

        echo -e "\n=== 3. STOPPING DATABASE ==="
        docker stop $(docker ps -q --filter ancestor=postgres:15)
        sleep 3

        echo -e "\n=== 4. TESTING HEALTH CHECK (DB OFFLINE - 503 SERVICE UNAVAILABLE) ==="
        curl -i http://localhost:8000/health

        echo -e "\n=== 5. SENDING SIGTERM (GRACEFUL SHUTDOWN) ==="
        kill -15 $APP_PID
        sleep 2
